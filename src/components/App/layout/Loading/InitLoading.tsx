import styles from './InitLoading.module.scss';

const InitLoading = () => {
    return (
        <div className={styles.container}>
            <span>Loading...</span>
        </div>
    );
};
export default InitLoading;
